# GNDSF — gndsf.ge (v0.1 · Phase 0–1 foundation)

საქართველოს სპორტული ცეკვების ეროვნული ფედერაციის პლატფორმა.
Next.js 15 (App Router) · TypeScript · Tailwind CSS v4 · PostgreSQL · Prisma (engine-less `client` mode + pg driver adapter).

## გაშვება ლოკალურად / Run locally

```bash
# 1. ბაზა (Docker)
docker compose up -d

# 2. დამოკიდებულებები
npm install

# 3. სქემა + სატესტო მონაცემები
cp .env.example .env
npx prisma migrate dev --name init   # creates real migrations on your machine
npm run db:seed

# 4. dev სერვერი
npm run dev                          # http://localhost:3000
```

თუ `prisma migrate` თქვენს გარემოში ვერ მუშაობს, `prisma/init.sql` იგივე სქემას ქმნის პირდაპირ:
`psql "$DATABASE_URL" -f prisma/init.sql`

## რა არის უკვე შესრულებული

- **სქემა**: სპორტსმენი / წყვილი / კლუბი / შეჯიბრება / შედეგი / ქულა / რეიტინგი —
  სრული lifecycle-დაცვით. შედეგები ებმის უცვლელ Entry ჩანაწერებს; პარტნიორის
  გაყრა/შეცვლა და კლუბის ტრანსფერი ისტორიას ვერასდროს დაარღვევს.
  `Entry`-ზე XOR check constraint (სოლო ან წყვილი — ზუსტად ერთი).
- **ქულების მოდელი**: ქულა ინდივიდუალურ სპორტსმენს მიჰყვება (WDSF მოდელი),
  წყვილის შედეგი ორ ჩანაწერს წერს; 12-თვიანი მოქმედების ვადა (`validUntil`).
  *კონფიგურირებადია — დაადასტურეთ ფედერაციის რეგლამენტით.*
- **რეიტინგის ჯობი**: `RankingEntry` სრულად გადაითვლება (truncate + rebuild),
  იდემპოტენტურია; ასაკობრივი კატეგორია ითვლება დაბადების წლიდან, არასდროს ინახება.
- **საჯარო გვერდები**: მთავარი, სპორტსმენების ბაზა (ძებნა/ფილტრი), პროფილი
  (პარტნიორობის ისტორია + შეჯიბრებების ისტორია), წყვილის გვერდი, რეიტინგი
  (ფილტრები URL-ში), სიახლეები, კალენდარი, კლუბები, დოკუმენტები, კონტაქტი.
- **ბრენდი**: ლოგო, favicon-ები, PWA მანიფესტი (Add to Home Screen), მუქი
  პრემიუმ დიზაინი ლოგოს ღვინისფერზე (#8B1520) აგებული.

## v0.2 — ავტორიზაცია და ადმინ პანელი

- **Auth.js (NextAuth v5)**: credentials login, JWT სესია, middleware იცავს /admin-ს.
- **RBAC**: როლი და კლუბის scope სესიაშივეა; `requireRole` / `clubScope` ჰელფერები.
- **ადმინ პანელი** (განზრახ ღია, უტილიტარული სტილი — Linear-ის ტიპის back-office):
  - დაფა: სტატისტიკა + აუდიტ-ლოგის ლენტა
  - სპორტსმენები: სია + რეგისტრაცია (MIN ავტომატურად); კლუბის მენეჯერი
    ხედავს/არეგისტრირებს მხოლოდ თავისი კლუბის ფარგლებში (server-side enforced)
  - წყვილები: შექმნა/გაყრა lifecycle-წესებით (ერთი მოქმედი პარტნიორობა
    სპორტსმენზე; გაყრა მხოლოდ პერიოდს ხურავს); ყველა ოპერაცია აუდიტ-ლოგში
- **სატესტო ანგარიშები** (პაროლი: `gndsf2026!`):
  - admin@gndsf.ge — SuperAdmin
  - secretary@gndsf.ge — გენერალური მდივანი
  - ritmi@gndsf.ge — კლუბ „რიტმის“ მენეჯერი (შეზღუდული scope)

## v0.3 — შეჯიბრებები და შედეგების პაიპლაინი (Phase 2)

- **შეჯიბრების ადმინი**: შექმნა (ტიპი → ქულის კოეფიციენტი ავტომატურად:
  რეგიონული ×0.8, ეროვნული ×1.0, საერთაშორისო ×1.5), ივენთების დამატება,
  გამოქვეყნება.
- **მონაწილეთა რეგისტრაცია**: რეგისტრაციისას სისტემა თავად აფიქსირებს
  კატეგორიისა და კლუბის სნეფშოთს (კატეგორია — უფროსი პარტნიორის მიხედვით).
- **შედეგების შეყვანა**: ერთი ფორმა ივენთზე; commit ერთ ტრანზაქციაშია:
  შედეგები → ქულები (წყვილზე ორ სპორტსმენს) → რეიტინგის სრული გადათვლა →
  აუდიტ-ლოგი. იდემპოტენტურია: ხელახალი commit ან შესწორება ქულებს არ აორმაგებს.
- **prevPosition**: რეიტინგში პოზიციის ცვლილება ინახება (▲▼ ისრებისთვის).
- **წვდომები**: შედეგებს მხოლოდ SuperAdmin/გენ. მდივანი შეიტანს; პრეზიდენტი
  ხედავს ყველაფერს რედაქტირების გარეშე; კლუბის მენეჯერს ეს განყოფილება არ უჩანს.
- **ქულების რეგლამენტი** ერთ ფაილშია: `src/lib/points.ts` — ოფიციალური
  ცხრილის მიღებისას მხოლოდ ეს იცვლება.
- ანგარიშები: admin@ / **president@** / secretary@ / ritmi@ gndsf.ge (პაროლი: gndsf2026!)

## v0.4 — სპორტსმენის კაბინეტი (Athlete Portal)

- **GID**: სპორტსმენის შიდა ნომერი (ყოფილი MIN) — ბაზაში, ადმინში და საჯარო
  გვერდებზე ყველგან GID-ია (ფორმატი: GID-1001). მიგრაცია:
  `prisma/migrations-manual/001_athlete_portal.sql` — **გაუშვით production
  ბაზაზე დეპლოიმდე!**
- **ATHLETE როლი + /cabinet**: სპორტსმენი შედის საკუთარ კაბინეტში და ხედავს:
  ფოტოს (ატვირთვით), მოქმედ ქულებს, რეიტინგის პოზიციებს (▲▼ ცვლილებით),
  ქულების დეტალიზაციას ვადებით და ტურნირების სრულ ისტორიას.
- **ფოტოს ატვირთვა**: Vercel-ზე → Vercel Blob (დაშბორდში შექმენით Blob store,
  ტოკენი ავტომატურად ჩაჯდება); ლოკალურად → /public/uploads fallback.
- **ანგარიშების გაცემა**: ადმინი → სპორტსმენები → „კაბინეტის ანგარიში“
  (ელფოსტა + დროებითი პაროლი). Demo: giorgi@demo.ge / tamar@demo.ge (gndsf2026!).
- **დაცვა**: სპორტსმენი /admin-ში ვერ შედის (→/cabinet), პერსონალი /cabinet-ში
  ვერ შედის (→/admin), ანონიმი → /login.

## Vercel-ზე განახლების ნაბიჯები

1. Neon/Postgres კონსოლში გაუშვით `prisma/migrations-manual/001_athlete_portal.sql`
2. `git push` — Vercel თავად ააწყობს (build script უკვე შეიცავს prisma generate-ს)
3. სურვილისამებრ: Vercel → Storage → Create Blob store (ფოტოებისთვის)

## v0.12 — ციფრული ლიცენზია (QR + Wallet)

- **/cabinet/card**: WDSF-სტილის მუქი ლიცენზია-ბარათი — ფოტო, სახელი (ka/en),
  GID, დაბადების თარიღი, კატეგორია, კლუბი, სეზონი, სტატუსი და QR კოდი.
- **QR → /verify/[gid]**: სკანირება ხსნის საჯარო შემოწმების გვერდს, რომელიც
  ცოცხალ სტატუსს აჩვენებს ბაზიდან (აქტიური/შეჩერებული, ფოტო, კლუბი).
  QR წაკითხვადობა დადასტურებულია რეალური სკანერით (zbar).
- **Wallet ღილაკები** ჩნდება env-ცვლადების დამატებისთანავე:

  Google Wallet (უფასო):
  1. pay.google.com/business/console → Google Wallet API → Issuer account
  2. Google Cloud: service account + JSON key; Wallet კონსოლში ამ ანგარიშს
     მიეცით წვდომა; შექმენით generic class ID: `gndsf_license`
  3. env: GOOGLE_WALLET_ISSUER_ID, GOOGLE_WALLET_SA_EMAIL, GOOGLE_WALLET_SA_KEY

  Apple Wallet (Apple Developer, $99/წ):
  1. developer.apple.com → Identifiers → Pass Type ID (pass.ge.gndsf.license)
  2. ამ ID-ზე სერტიფიკატი + private key; ჩამოტვირთეთ Apple WWDR G4 სერტიფიკატი
  3. env (base64 PEM): APPLE_PASS_SIGNER_CERT, APPLE_PASS_SIGNER_KEY,
     APPLE_WWDR_CERT, APPLE_PASS_TYPE_ID, APPLE_TEAM_ID
     (სურვილით: APPLE_PASS_KEY_PASSPHRASE)

## v0.13 — მოდულური უფლებები + სტუდიის თვითრეგისტრაცია

- **`src/lib/permissions.ts`**: ცენტრალური capability-მატრიცა (Role →
  Capability[]). ეს ფაილია ერთადერთი ადგილი, სადაც წერია "რომელ როლს
  რისი გაკეთება შეუძლია" — უფლების შესაცვლელად აღარ არის საჭირო
  ათეულობით ფაილში სხვადასხვაგვარად ჩაწერილი როლის მასივების ძებნა.
  `REGISTRY_ADMINS`/`RESULT_ADMINS` (`src/lib/rbac.ts`) ახლა ამ
  მატრიციდან გამოითვლება, ასე რომ ძველი იმპორტები არ გატეხილა.
- **კლუბის მენეჯერი აღარ არის მხოლოდ დამთვალიერებელი**: შეუძლია
  საკუთარი კლუბის სპორტსმენის რეგისტრაცია (დაუყოვნებლივ, ისე როგორც
  ადმინისთვის) და საკუთარი კლუბის ორი თავისუფალი სპორტსმენის
  შეწყვილება/გაყრა — ორივე სერვერის მხარეს არის შეზღუდული საკუთარ
  კლუბზე (ფორმის მანიპულირებაც კი ვერ გასცდება საკუთარ კლუბს).
- **სტუდიის თვითრეგისტრაცია** (`/studio/apply`): საჯარო ფორმა, სადაც
  სტუდია თავად აყენებს კლუბის მონაცემებს + ელფოსტა/პაროლს. მოთხოვნა
  ინახება `ClubRegistration`-ში სტატუსით `PENDING`. ფედერაციის ადმინი
  ამტკიცებს `/portal/club-registrations`-ზე — დამტკიცება ერთ
  ტრანზაქციაში ქმნის Club-ს და CLUB_MANAGER User-ს (იმავე პაროლით, რაც
  სტუდიამ თავად აირჩია), ასე რომ სტუდია დაუყოვნებლივ შედის.
  მიგრაცია: `prisma/migrations-manual/008_club_registration.sql` —
  **გაუშვით production ბაზაზე დეპლოიმდე!**
- **ფოსტის წვდომა გამკაცრდა**: server actions-ში `MAIL_ACCESS`
  capability რეალურად მოწმდება (ადრე მხოლოდ ნავიგაციაში იყო დამალული
  სხვა როლებისთვის, თვითონ action-ები ნებისმიერ staff-ს უშვებდა).
- **`/login`-ზე დამატებულია ბმული** `/studio/apply`-ზე.
- **მნიშვნელოვანი**: `src/middleware.ts`-ში "საიტი განახლების
  პროცესშია" ბლოკი გაფართოვდა — /login, /portal, /cabinet, /e-card,
  /studio და /verify ახლა მაინც ხელმისაწვდომია საჯარო მარკეტინგული
  გვერდების (მთავარი, სპორტსმენები, სიახლეები და ა.შ.) დაბლოკვის
  დროსაც, რადგან წინააღმდეგ შემთხვევაში არც ადმინს, არც სტუდიას
  არსად ექნებოდა შესვლა/რეგისტრაციის საშუალება.

## v0.14 — ფედერაციის დადასტურებული ქულების ცხრილი

- **`src/lib/points.ts`** მთლიანად ჩანაცვლდა. ძველი placeholder
  (`PLACEMENT_POINTS`/`pointsFor`, ადგილზე დაფუძნებული ბრტყელი ცხრილი
  × კოეფიციენტი) აღარ გამოიყენება ქულების დასათვლელად — მის მაგივრად
  არის ორი ფუნქცია, ორივე პირდაპირ აგებული ფედერაციის დადასტურებულ
  PDF-ცხრილებზე (`axali soloebi.pdf` გვ.1 და `წყვილების რეიტინგი.pdf`):
  - `getSoloScore(participants, ageCategory, danceClass, place)` —
    სოლოსთვის: ცხრილი დამოკიდებულია მონაწილეთა რაოდენობის რეინჯზე
    (0-6/7-12/13-24/24+) და კლასზე (A/B/C/D). 24+ რეინჯში მე-7
    ადგილიდან ყველა ადგილი ფიქსირებული 0.5 ქულაა. კომბინაცია, რომელიც
    ცხრილში საერთოდ არ არსებობს (მაგ. D კლასი 7-12 რეინჯში, ან
    ადგილი > 6 არა-24+ რეინჯში), ისვრის შეცდომას — არ აბრუნებს ჩუმად 0-ს.
  - `getCoupleScore(category, place)` — წყვილებისთვის: ცხრილი
    დამოკიდებულია კატეგორიაზე (6 Dance / Rising Star / Juvenile I+II /
    Junior I / Junior II / Youth / Adult) და ადგილზე (რეინჯები 1-დან
    97-192-მდე, მთელი ტურნირის ველის ზომის მიხედვით).
  - `pointsCoefficient`/`TYPE_COEFFICIENTS` (კონკურსის ტიპის კოეფიციენტი)
    დარჩა სქემაში/UI-ში უცვლელად, მაგრამ **აღარ მონაწილეობს ქულის
    გამოთვლაში** — დადასტურებული ცხრილი აბსოლუტურ ქულებს იძლევა ტიპის
    მიუხედავად. თუ მომავალში გინდათ საერთაშორისო/რეგიონული შეჯიბრის
    დამატებითი წონა, ეს ცალკე გადასაწყვეტია.
- **სქემაში ახალი ველები** (`CompEvent`-ზე): `danceClass` (`DanceClass`
  enum: A/B/C/D, მხოლოდ სოლოსთვის) და `coupleCategory` (`CoupleCategory`
  enum: SIX_DANCE/RIZING_STAR/JUVENILE_1_2/JUNIOR_1/JUNIOR_2/YOUTH/ADULT,
  მხოლოდ წყვილებისთვის) — ორივე optional, მაგრამ **სავალდებულოა
  შედეგების დაფიქსირებამდე** (`commitResults` შეცდომას დააბრუნებს
  ცხადი ტექსტით, თუ ივენთს არცერთი არ აქვს მითითებული).
  უნიკალური constraint გაფართოვდა, რომ ერთ ასაკობრივ კატეგორია/
  დისციპლინაში რამდენიმე კლასის/კატეგორიის ივენთი შეიძლებოდეს ერთდროულად.
  მიგრაცია: `prisma/migrations-manual/009_scoring_categories.sql` —
  **გაუშვით production ბაზაზე დეპლოიმდე!**
- **`/portal/competitions/[id]`**: ივენთის დამატების ფორმას დაემატა
  ორი select — "კლასი" (სოლოსთვის) და "კატეგორია" (წყვილებისთვის).
  ივენთის სათაურში წითლად ეწერება გაფრთხილება, თუ კლასი/კატეგორია
  ჯერ არ არის არჩეული. შედეგების დაფიქსირებისას წარმოქმნილი
  შეცდომა (ცხრილში არარსებული კომბინაცია, ან ჯერ არჩეული არ არის
  კლასი/კატეგორია) გვერდზე წითელი ბანერით ჩანს, ნაცვლად უბრალო 500
  გვერდისა.
- სატესტო წყვილი დადასტურდა: 7 მონაწილე, A კლასი, ადგილი 1 → 55.
  სოლოს ეს ტესტ-ქეისი რეალურად "7-12" რეინჯს ეხება (0-6 არა), 55 სწორედ
  იქიდანაა. წყვილის ტესტ-ქეისი: Adult კატეგორია, ადგილი 9 (რეინჯი
  7-12) → 16.

## v0.15 — TopTurnier-ის შედეგების იმპორტი

- **პრობლემა, რომელსაც ეს წყვეტს**: სკრუტინიორები ტურნირს ატარებენ
  TopTurnier-ის (topturnier.de) საშუალებით და შედეგებს აქვეყნებენ HTML
  გვერდად (მაგ. dancesportinfo.lt-ზე). აქამდე ეს შედეგები ხელით უნდა
  გადაეტანა ადმინს placement-ების ველებში — ტურის მიხედვით ვინ გავიდა
  და ვინ არა საერთოდ არ ფიქსირდებოდა. ეს ვერსია მას ავტომატიზირებს.
- **რატომ HTML და არა PDF**: TopTurnier-ის HTML ექსპორტი უკვე
  სტრუქტურირებული ცხრილია (არა pixel/OCR), და მისი "Competition Listing"
  სექცია თავისთავად შეიცავს ორივეს ერთად — საბოლოო ადგილს (skating
  სისტემით უკვე გამოთვლილს, ტოლობების ჩათვლით, მაგ. "13.-14.") **და**
  რომელ ტურამდე მივიდა თითოეული. ანუ **სქეითინგის ალგორითმი არ გვჭირდება
  ხელახლა დავწეროთ** — TopTurnier უკვე თვითონ თვლის მას, ჩვენ უბრალოდ
  ვკითხულობთ მის უკვე-გამოთვლილ შედეგს.
- **`src/lib/topturnierParse.ts`** (ახალი) — სუფთა ფუნქცია
  `parseTopturnier(rawText)`. ადმინი აკოპირებს მთელ შედეგების გვერდს
  (Cmd/Ctrl+A → Cmd/Ctrl+C ბრაუზერიდან) და სვამს textarea-ში;
  ბრაუზერი ტექსტად აქცევს ცხრილს ტაბ-გამოყოფილ მწკრივებად, პარსერი კი
  კითხულობს "Competition Listing" სექციას (round-ჯგუფების სათაურები:
  "Final"/"3rd round"/"2nd round"/"1st round"... + თითო მწკრივი
  `რანგი\tსტარტნომერი\tსახელი\tქვეყანა`). თუ ეს სექცია არაა (მოკლე
  ტურნირი წინასწარი ტურების გარეშე), fallback — "Result of the Final".
  **გადამოწმებულია რეალურ მონაცემზე**: Batumi Open 2026, "WDSF Open
  Solo Latin Jun. II" (27 მონაწილე, dancesportinfo.lt) — იხ.
  `scripts/test-topturnier-parse.ts` (`npx tsx scripts/test-topturnier-parse.ts`),
  ყველა 27 მონაწილის ადგილი/ტური ზუსტად ემთხვევა რეალურ შედეგს.
- **`src/lib/nameMatch.ts`** (ახალი) — TopTurnier-ის თავისუფალი-ტექსტის
  სახელს (მაგ. "Elene Dandurishvili") ადარებს ჩვენი ბაზის უკვე
  დარეგისტრირებულ Entry-ebს (Jaccard მსგავსობა სახელის ტოკენებზე) და
  სთავაზობს საუკეთესო დამთხვევას — მაგრამ ადმინს ყოველთვის რჩება
  ხელით არჩევის select, თუ ავტომატური დამთხვევა არასწორია ან არ იძებნება.
- **`src/lib/results.ts`**: `commitEventResults`-ს დაემატა ოფციონალური
  `roundsReached` თითო placement-ზე (ინახება უკვე არსებულ
  `Result.roundsReached` ველში — **მიგრაცია არ სჭირდება**, ეს ველი
  სქემაში/ბაზაში უკვე იყო, უბრალოდ არასდროს ივსებოდა).
- **`src/app/portal/competitions/[id]/actions.ts`**: ორი ახალი
  server action — `previewTopturnierImport(eventId, rawText)` (პარსავს
  და ასახავს Entry-ებთან) და `commitTopturnierImport(eventId, rows)`
  (იძახებს იმავე `commitEventResults`-ს, რასაც ხელით შეყვანაც იყენებს —
  ქულების დათვლა და რეიტინგის გადათვლა იდენტურია).
- **`TopturnierImport.tsx`** (ახალი client component) — თითო ივენთის
  ბოლოში ღილაკი "TopTurnier-დან შემოტანა": textarea → "დაპარსვა და
  გადახედვა" → სრულად სარედაქტირებელი გადახედვის ცხრილი (თითო
  მწკრივზე select მონაწილის დასადასტურებლად/შესაცვლელად, ადგილი და
  ტური — ორივე ხელახლა რედაქტირებადი override-ის სახით, ტოლობის
  შემთხვევაში ნაჩვენებია დიაპაზონი) → "დაფიქსირება".
- **ხელით შეყვანის ცხრილს** დაემატა "ტური" სვეტი (`roundsReached_<entryId>`),
  სავსებით ოფციონალური.
- **განზრახ არ არის დაპარსილი** ამ ვერსიაში: "Table of results" (თითო
  მსაჯის ნედლი ნიშნები ტურების მიხედვით), "Report of Final Round" და
  "Skating report" (ცეკვა-ცეკვად skating-გამოთვლა) — ეს text-ექსპორტში
  მრავალხაზიან უჯრედებად იშლება (მყიფეა სუფთა ტექსტიდან საიმედოდ
  დასაპარსად), და "Competition Listing" საკმარისია "ვინ გავიდა და ვინ
  არა" ამოცანისთვის. მსაჯების ნედლი ნიშნების გამჭვირვალობის ფუნქცია
  შეიძლება მომავალში დაემატოს ცალკე.

## შემდეგი ფაზები

- Phase 3–4: საჯარო შეჯიბრების გვერდები + CMS (სიახლეები/კალენდარი/დოკუმენტები ადმინიდან)
- Phase 4: CMS (სიახლეები/კალენდარი/დოკუმენტები ადმინიდან)
- Phase 5: i18n (ka/en), ელფოსტის ვიჯეტი, SEO/OG, წარმადობის აუდიტი

## შენიშვნა Prisma-ზე

გენერატორი მუშაობს `engineType = "client"` რეჟიმში — Rust ბინარების გარეშე,
`@prisma/adapter-pg`-ით. ეს უფრო მსუბუქია serverless გარემოსთვისაც (Vercel).
